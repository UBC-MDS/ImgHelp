def ColorConv(img, color):
    """
    ColorCov converts an image to different color.
    Parameters
    ----------
    img : numpy.array
        Input a 2D np.array image for convertion.
    color : string
        Color aim to convert: 'gray', 'black'
    Returns
    -------
    numpy.array
        The converted image as 2D np.array.
    Examples
    --------
    >>> ColorConv(img, 'gray')
    """